import { CalendarDays, ClipboardList, LayoutDashboard, LogOut, Menu, Scissors, Users, X } from 'lucide-react'
import { NavLink, Outlet } from 'react-router-dom'; import { useState } from 'react'; import { useAuth } from '../../context/useAuth'; import { formatToday } from '../../lib/date'
const links = [{ to: '/', label: 'Resumen', icon: LayoutDashboard, roles: ['Administrador', 'Empleado', 'Cliente'] }, { to: '/citas', label: 'Citas', icon: CalendarDays, roles: ['Administrador', 'Empleado', 'Cliente'] }, { to: '/servicios', label: 'Servicios', icon: Scissors, roles: ['Administrador', 'Empleado', 'Cliente'] }, { to: '/adicionales', label: 'Adicionales', icon: Scissors, roles: ['Administrador', 'Empleado', 'Cliente'] }, { to: '/especialidades', label: 'Especialidades', icon: ClipboardList, roles: ['Administrador', 'Empleado', 'Cliente'] }, { to: '/roles', label: 'Roles', icon: ClipboardList, roles: ['Administrador'] }, { to: '/estados-cita', label: 'Estados de cita', icon: ClipboardList, roles: ['Administrador', 'Empleado', 'Cliente'] }, { to: '/empleados', label: 'Empleados', icon: Users, roles: ['Administrador'] }, { to: '/horarios', label: 'Horarios', icon: ClipboardList, roles: ['Administrador'] }, { to: '/restricciones', label: 'Restricciones', icon: ClipboardList, roles: ['Administrador'] }, { to: '/agenda', label: 'Agenda diaria', icon: ClipboardList, roles: ['Administrador'] }, { to: '/perfil', label: 'Mi perfil', icon: Users, roles: ['Administrador', 'Empleado', 'Cliente'] }]
export function AppLayout() { const [open, setOpen] = useState(false); const { user, logout } = useAuth(); const visibleLinks = links.filter(link => user && link.roles.includes(user.rol)); return <div className="app-frame"><aside className={`sidebar ${open ? 'sidebar-open' : ''}`}><div className="brand"><div className="brand-mark"><CalendarDays size={20} /></div><div><strong>MediInfo</strong><span>Gestión de citas</span></div><button className="icon-button mobile-only" onClick={() => setOpen(false)}><X size={19} /></button></div><div className="workspace-label">Espacio de trabajo</div><nav className="side-nav">{visibleLinks.map(({ to, label, icon: Icon }) => <NavLink key={to} to={to} end={to === '/'} onClick={() => setOpen(false)}><Icon size={18} />{label}</NavLink>)}</nav><div className="sidebar-bottom"><div className="user-mini"><div className="avatar">{user?.nombre[0]?.toUpperCase()}</div><div><strong>{user?.nombre}</strong><span>{user?.rol}</span></div></div><button className="logout-button" onClick={logout}><LogOut size={16} />Cerrar sesión</button></div></aside><main className="main-content"><header className="topbar"><button className="icon-button mobile-only" onClick={() => setOpen(true)}><Menu size={21} /></button><div className="topbar-context"><span>Centro de atención</span><strong>Panel de control</strong></div><div className="topbar-date">{formatToday()}</div></header><Outlet /></main></div> }
