import { CatalogPage } from '../modules/catalog/CatalogPage'
type Restriction = { id: number; fecha: string; horaInicio?: string; horaFin?: string; motivo: string; empleado?: { nombre: string }; tipo?: { nombre: string } }
export function RestrictionsPage() { return <CatalogPage<Restriction> title="Restricciones" eyebrow="Disponibilidad" description="Consultá los bloqueos generales y específicos del establecimiento." endpoint="/restricciones-horario" empty="No hay restricciones registradas" columns={[{ label: 'Fecha', render: x => x.fecha }, { label: 'Horario', render: x => `${x.horaInicio || 'Todo el día'}${x.horaFin ? ` - ${x.horaFin}` : ''}` }, { label: 'Aplica a', render: x => x.empleado ? `${x.empleado.nombre} (empleado)` : 'Establecimiento' }, { label: 'Motivo', render: x => x.motivo }]} /> }
