import { ArrowLeft, LoaderCircle } from 'lucide-react'; import { useEffect, useState, type FormEvent } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { api } from '../services/api'; import './service-form.css'
type Option = { id: number; nombre: string }; function unwrap<T>(value: T | { data?: T }): T { return (value && typeof value === 'object' && 'data' in value ? value.data : value) as T }
export function NewEmployeePage() { const navigate = useNavigate(); const [users, setUsers] = useState<Option[]>([]); const [specialties, setSpecialties] = useState<Option[]>([]); const [services, setServices] = useState<Option[]>([]); const [selected, setSelected] = useState<number[]>([]); const [form, setForm] = useState({ usuarioId: '', especialidadId: '', codigoEmpleado: '', descripcion: '' }); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(''); useEffect(() => { Promise.all([api.list<Option[] | { data: Option[] }>('/usuarios?rol=Empleado'), api.list<Option[] | { data: Option[] }>('/especialidades'), api.list<Option[] | { data: Option[] }>('/servicios/activos')]).then(([u, s, v]) => { const nextUsers = unwrap(u) || []; const nextSpecialties = unwrap(s) || []; setUsers(nextUsers); setSpecialties(nextSpecialties); setServices(unwrap(v) || []); setForm(current => ({ ...current, usuarioId: String(nextUsers[0]?.id || ''), especialidadId: String(nextSpecialties[0]?.id || '') })) }).catch(e => setError(e.message)).finally(() => setLoading(false)) }, []); const toggle = (id: number) => setSelected(current => current.includes(id) ? current.filter(x => x !== id) : [...current, id]); async function submit(event: FormEvent) { event.preventDefault(); if (!form.codigoEmpleado || !form.usuarioId || !form.especialidadId || selected.length === 0) return setError('Completá los campos y asigná al menos un servicio.'); if (!/^[A-Za-z0-9_-]+$/.test(form.codigoEmpleado)) return setError('El código solo puede contener letras, números, guiones y guion bajo.'); setSaving(true); setError(''); try { await api.create('/empleados', { usuarioId: Number(form.usuarioId), especialidadId: Number(form.especialidadId), codigoEmpleado: form.codigoEmpleado, descripcion: form.descripcion || null, servicioIds: selected }); navigate('/empleados') } catch (e) { setError((e as Error).message) } finally { setSaving(false) } } if (loading) return <section className="page-shell"><div className="empty-state"><LoaderCircle size={24}/><p>Cargando opciones...</p></div></section>; return <section className="page-shell"><Link className="back-link" to="/empleados"><ArrowLeft size={14}/> Volver a empleados</Link><div className="section-heading"><div><p className="eyebrow">Equipo</p><h1>Nuevo empleado</h1><p className="lead">Asigná los servicios que puede atender.</p></div></div><form className="service-form panel" onSubmit={submit}><label>Usuario *<select required value={form.usuarioId} onChange={e => setForm({ ...form, usuarioId: e.target.value })}>{users.map(x => <option key={x.id} value={x.id}>{x.nombre}</option>)}</select></label><label>Especialidad *<select required value={form.especialidadId} onChange={e => setForm({ ...form, especialidadId: e.target.value })}>{specialties.map(x => <option key={x.id} value={x.id}>{x.nombre}</option>)}</select></label><label>Código del empleado *<input required value={form.codigoEmpleado} onChange={e => setForm({ ...form, codigoEmpleado: e.target.value })} placeholder="MED-001"/></label><label>Descripción<textarea value={form.descripcion} onChange={e => setForm({ ...form, descripcion: e.target.value })}/></label><fieldset><legend>Servicios asignados *</legend>{services.map(x => <label className="check-option" key={x.id}><input type="checkbox" checked={selected.includes(x.id)} onChange={() => toggle(x.id)}/>{x.nombre}</label>)}</fieldset>{error && <div className="form-error">{error}</div>}<button className="primary-button" disabled={saving}>{saving ? 'Guardando...' : 'Crear empleado'}</button></form></section> }
